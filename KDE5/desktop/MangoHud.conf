# MangoHud v0.8.3+ 配置 (NVIDIA NVML)
# pci_dev 由 entrypoint.sh 运行时动态注入，不要在此文件中设置
legacy_layout=false
gpu_stats
gpu_temp
gpu_core_clock
gpu_mem_clock
gpu_power
cpu_stats
cpu_temp
cpu_mhz
ram
vram
fps
frametime
frame_timing=1
